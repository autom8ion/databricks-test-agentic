# Layer 1 (tests/unit/): plain PySpark for local, no-cluster unit tests.
#
# Databricks Connect and PySpark cannot be installed in the same Python
# environment (Databricks Connect replaces the pyspark package with a
# remote-only client) — install this in a SEPARATE venv from requirements.txt.
#
# Needs pyspark>=4.2 (Python 3.14): pyspark 4.0.x's pandas-on-Spark compat
# layer (a transitive import of pyspark.testing.assertDataFrameEqual)
# breaks on pandas>=2.2, and pandas<2.2 has no Python 3.14 wheel. pyspark
# 4.2.x resolves against current pandas (with a FutureWarning about
# pandas>=3.0 support, not an error).
pyspark[sql]>=4.2,<5
pytest>=8
